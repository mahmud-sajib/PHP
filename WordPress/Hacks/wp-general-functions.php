<?php

/*

General purpose functions:

1. get_template_directory() returns the full system path (directory)

2. get_template_directory_uri() returns the web url

3. rewind_posts() rewinds the loop posts in order to re-use the same query in different locations on a page

4. wp_reset_postdata() restores the $post global to the current post in the main query after looping through a separate custom query

5. get_the_id() retrieves the ID of the current item in the WordPress Loop.

6. get_post_meta($thumbnail_id) retrieves post meta field for a post.

7. get_template_part( $directory_path ) loads a template part into a template.

8. do_shortcode('[shortcodeName]') displays the shortcode's content on a page or post

9. get_theme_mod( $name, $default ) retrieves a modification setting for the current theme

*/

?>
