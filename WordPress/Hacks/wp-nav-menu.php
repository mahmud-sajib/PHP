<?php

/*

Nav Menu specific rules:

1. register_nav_menu( $location, $description ) registers a single custom Navigation Menu in the custom menu editor. Example:

    register_nav_menu( 'primary', __( 'Primary Menu', 'simplyroom' ) );

    __( 'Primary Menu', 'simplyroom' ) in this function the first parameter is the string we would like to translate and the second parameter is the text domain the translation will use.

2. wp_nav_menu() displays a navigation menu on the Front-End.

*/

?>
