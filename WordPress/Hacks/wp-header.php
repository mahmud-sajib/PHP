<?php

/*

Header specific rules:

1. bloginfo('charset') displays the characterset of the document

2. language_attributes() displays the language attributes for the html tag

3. wp_head() is used by WordPress core to insert crucial elements into the document (e.g., scripts, styles, and meta tags). 

4. body_class() display the classes for the body element.

5. has_custom_logo() determines whether the site has a custom logo

6. the_custom_logo() displays the custom logo()

7. home_url() retrieves the home URL for the current site

8. bloginfo('name') displays the blog's title in a tag.

9. get_header() includes the header.php template file from your current theme's directory

10. get_footer() includes the footer.php template file from your current theme's directory

*/


?>
